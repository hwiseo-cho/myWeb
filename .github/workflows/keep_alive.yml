name: Render Keep Alive

on:
  schedule:
    # 10분마다 실행 (UTC 기준)
    - cron: '*/10 * * * *'
  workflow_dispatch: # 수동 실행 버튼 활성화

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send a heart beat
        run: curl -I https://myweb-2gl6.onrender.com
